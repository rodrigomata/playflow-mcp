# Your PlayFlow server API key (required). Starts with "pf_".
# Find/regenerate it in the PlayFlow Dashboard under Project Settings.
PLAYFLOW_API_KEY=

# Optional overrides:
# PLAYFLOW_BASE_URL=https://api.computeflow.cloud
# PLAYFLOW_TIMEOUT=30
